import React from 'react'

import {
	PageTemplate,
	Header,
	Footer,
	Heading
} from 'components'

const ResetPasswordView = () => {
  return (
    <PageTemplate
    	header={<Header />}
    	footer={<Footer />}
    	title='Reset Password'
    	>
    </PageTemplate>
  )
}

export default ResetPasswordView
